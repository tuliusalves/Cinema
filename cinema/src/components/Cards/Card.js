import styled from "styled-components";
const CardContainer = styled.div`
  // Seus estilos
`;

function Card({ titulo, capa, id }) {
  return <CardContainer>{/* Seu conteúdo do card */}</CardContainer>;
}

export default Card;
